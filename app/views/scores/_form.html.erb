<%= form_for(@score) do |f| %>
  <% if @score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@score.errors.count, "error") %> prohibited this score from being saved:</h2>

      <ul>
      <% @score.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student %><br />
    <%= select_tag(:set_user,options_from_collection_for_select(
    	User.find(:all),:id,:user_name)) %>
  </div>
  
  <div class="field">
    <%= f.label :subject %><br />
    <%= select_tag(:set_sub,options_from_collection_for_select(
    	Subject.find(:all),:id,:sub_name)) %>
  </div>
  
  <div class="field">
    <%= f.label :tensu %><br />
    <%= f.number_field :tensu %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
